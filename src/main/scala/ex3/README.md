**Hello World with Remote**

_TODO_

* Create a new Actor System passing `app2.config` as configuration file
* Retrieve the actors of the existing Actor System
* Send them an `HelloMessage` passing as content their name 
